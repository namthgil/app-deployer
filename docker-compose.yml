services:
  app-deployer:
    build: .
    container_name: app-deployer
    restart: unless-stopped
    ports:
      - "${HOST_IP:-0.0.0.0}:${HOST_PORT:-80}:80"
    volumes:
      # Apps persist on the host at ./apps
      - ./apps:/var/www/apps
    environment:
      - APPS_DIR=/var/www/apps
